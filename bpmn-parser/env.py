# simplified environment configuration
# only keep the necessary configuration for parsing BPMN

# system variables (can be empty, keep interface compatibility)
SYSTEM_VARS = {
    "parser_name": "BPMN Simplified Parser",
    "version": "1.0.0"
}

# data source configuration (can be empty, keep interface compatibility)
DS = {} 